[build]
builder = "nixpacks"

[install]
cmds = ["python -m venv .venv", ". .venv/bin/activate", "pip install -r requirements.txt"]

# O comando de start será gerenciado pelo Procfile ou configurado manualmente no Railway.
